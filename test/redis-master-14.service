[Unit]
Description=Redis In-Memory Data Store (Master 50014)
After=network.target

[Service]
User=redis
Group=redis
ExecStart=/usr/bin/redis-server /etc/redis/redis-50014.conf --supervised systemd
ExecStop=/usr/bin/redis-cli -p 50014 shutdown
Restart=always

[Install]
WantedBy=multi-user.target