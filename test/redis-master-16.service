[Unit]
Description=Redis In-Memory Data Store (Master 50016)
After=network.target

[Service]
User=redis
Group=redis
ExecStart=/usr/bin/redis-server /etc/redis/redis-50016.conf --supervised systemd
ExecStop=/usr/bin/redis-cli -p 50016 shutdown
Restart=always

[Install]
WantedBy=multi-user.target