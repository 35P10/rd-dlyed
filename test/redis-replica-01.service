[Unit]
Description=Redis In-Memory Data Store (Slave 50017)
After=network.target

[Service]
User=redis
Group=redis
ExecStart=/usr/bin/redis-server /etc/redis/redis-50017.conf --supervised systemd
ExecStop=/usr/bin/redis-cli -p 50017 shutdown
Restart=always

[Install]
WantedBy=multi-user.target