[Unit]
Description=Redis In-Memory Data Store (Slave 50018)
After=network.target

[Service]
User=redis
Group=redis
ExecStart=/usr/bin/redis-server /etc/redis/redis-50018.conf --supervised systemd
ExecStop=/usr/bin/redis-cli -p 50018 shutdown
Restart=always

[Install]
WantedBy=multi-user.target