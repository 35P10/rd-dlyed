[Unit]
Description=Redis In-Memory Data Store (Slave 50028)
After=network.target

[Service]
User=redis
Group=redis
ExecStart=/usr/bin/redis-server /etc/redis/redis-50028.conf --supervised systemd
ExecStop=/usr/bin/redis-cli -p 50028 shutdown
Restart=always

[Install]
WantedBy=multi-user.target