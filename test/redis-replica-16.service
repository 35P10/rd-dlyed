[Unit]
Description=Redis In-Memory Data Store (Slave 50032)
After=network.target

[Service]
User=redis
Group=redis
ExecStart=/usr/bin/redis-server /etc/redis/redis-50032.conf --supervised systemd
ExecStop=/usr/bin/redis-cli -p 50032 shutdown
Restart=always

[Install]
WantedBy=multi-user.target